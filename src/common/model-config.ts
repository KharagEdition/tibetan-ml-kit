export interface ModelConfig {
  /**
   * The API key for the model.
   */
  apiKey: string;
  /**
   * The model name.
   */
  modelName: string;
  /**
   * prompt message for the model
   */
  promptMessage?: string;
}
